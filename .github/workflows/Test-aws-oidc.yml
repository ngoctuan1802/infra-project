name: 00. Test AWS OIDC

on:
  workflow_dispatch:   # chạy thủ công từ GitHub UI

jobs:
  aws-test:
    runs-on: ubuntu-latest
    permissions:
      id-token: write   # cần cho OIDC
      contents: read

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Configure AWS credentials via OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::491085416582:role/GitHubActions-Role # get on AWS IAM console
          aws-region: us-east-2

      - name: Verify identity
        run: aws sts get-caller-identity

  slack-notify:
    name: Notify Slack
    needs: terraform
    if: always()
    uses: ./.github/workflows/slack-notify.yml
    with:
      status: ${{ needs.terraform.result }}
      stage: "Terraform ${{ github.event.inputs.action }}"
      environment: ${{ github.event.inputs.environment }}
      repository: ${{ github.repository }}
      actor: ${{ github.actor }}
      run_url: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
      summary_file: terraform.log    # ✅ tùy chọn: trỏ tới file plan hoặc error log
    secrets:
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
